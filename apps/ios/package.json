{
  "name": "@fittrackr/ios",
  "version": "0.0.1",
  "private": true,
  "description": "Native iOS shell. Loads the self-hosted web app and adds a session Live Activity.",
  "scripts": {
    "cap:add": "cap add ios",
    "cap:sync": "cap sync ios",
    "cap:open": "cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  }
}
