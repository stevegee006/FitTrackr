{
  "name": "@fittrackr/ios",
  "version": "0.0.1",
  "private": true,
  "description": "Native iOS shell. Loads the deployed web app and adds a rest-timer Live Activity.",
  "scripts": {
    "add": "cap add ios",
    "sync": "cap sync ios",
    "open": "cap open ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  }
}
